{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Triage result (exeris-sdk)",
  "description": "The bundle's shape, narrowed to this repository's classes and roles. The vocabulary is not invented here: `task_class` is the enum `.agents/skills/exeris-sdk-task-classifier/SKILL.md` already publishes as its output contract, and this file is where that contract becomes checkable.",
  "allOf": [
    { "$ref": "../vendor/exeris-agents-1.4.0/schemas/triage-result.base.schema.json" },
    {
      "properties": {
        "task_class": {
          "enum": [
            "ANNOTATION_CONTRACT",
            "AST_WIRE_FORMAT",
            "UI_KIT",
            "STABILITY_DEPRECATION",
            "DOCS_ADR",
            "PUBLISH_READINESS",
            "BUILD_INVARIANTS",
            "MULTI_DOMAIN"
          ]
        },
        "primary_agent": {
          "enum": [
            "exeris-sdk-router",
            "exeris-sdk-architect",
            "exeris-sdk-implementer",
            "exeris-sdk-verification",
            "exeris-sdk-docs-adr"
          ]
        }
      }
    }
  ]
}
