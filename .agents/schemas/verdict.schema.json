{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Review verdict (exeris-sdk)",
  "description": "The bundle's shape, narrowed to this repository's reviewing roles and its surfaces. The three-state `decision` and the `checks_run` contract are the base's; only the vocabulary is local. `scope_class` is finer than the four classes ADR-085 §E.16 fixes for a pull-request body, because a review answers which surface broke and a body answers how far the change reaches.",
  "allOf": [
    {
      "$ref": "../vendor/exeris-agents-2.1.0/schemas/verdict.base.schema.json"
    },
    {
      "properties": {
        "agent": {
          "enum": [
            "exeris-sdk-architect"
          ],
          "description": "One role, because one role emits. `exeris-sdk-architect` is the only profile naming this schema in `output`. The CI review is the organisation's, under its own role and schema, and does not validate against this file. Widening the enum ahead of a second emitter would let a verdict validate under a role that has never produced one."
        },
        "scope_class": {
          "enum": [
            "annotation-surface",
            "ast-wire-format",
            "composition",
            "ui-kit",
            "build-publish",
            "docs-only",
            "agent-layer",
            "mixed"
          ]
        },
        "findings": {
          "items": {
            "$ref": "../vendor/exeris-agents-2.1.0/schemas/verdict.base.schema.json#/properties/findings/items",
            "unevaluatedProperties": false
          }
        },
        "checks_run": {
          "items": {
            "$ref": "../vendor/exeris-agents-2.1.0/schemas/verdict.base.schema.json#/properties/checks_run/items",
            "unevaluatedProperties": false
          }
        },
        "handoffs": {
          "items": {
            "$ref": "../vendor/exeris-agents-2.1.0/schemas/verdict.base.schema.json#/properties/handoffs/items",
            "unevaluatedProperties": false
          }
        }
      }
    }
  ],
  "unevaluatedProperties": false
}
